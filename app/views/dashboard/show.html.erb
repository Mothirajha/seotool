<%= render 'queries/form'%>

<strong> Download As </strong>
<%= link_to "CSV", queries_path(format: "csv") %> | 
<%= link_to "XLS", queries_path(format: "xls") %>

<%if @queries.present? %>
  <%= render 'keyword_position', queries: @queries %>
<%end%>
