<h1>Listing queries</h1>

<table id='queries' class='display'>
  <thead>
    <tr>
      <th>Keyword</th>
      <th>Campaign</th>
      <th>Search engine</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @queries.each do |query| %>
      <tr>
        <td><%= query.keyword %></td>
        <td><%= query.campaign.name %></td>
        <td><%= query.search_engine.engine %></td>
        <td><%= link_to 'Show', query %> | <%= link_to 'Edit', edit_query_path(query) %> |<%= link_to 'Destroy', query, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Query', new_query_path %>


